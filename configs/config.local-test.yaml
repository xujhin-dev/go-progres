server:
  port: "8080"
  mode: "debug"

database:
  host: "localhost"
  user: "postgres"
  password: "postgres"
  dbname: "postgres_test"
  port: "5432"
  sslmode: "disable"
  timezone: "Asia/Shanghai"

redis:
  addr: "localhost:6379"
  password: ""
  db: 1

jwt:
  secret: "local-test-secret-key"
  expire: 24

# 环境配置
app:
  env: "local-test"
  debug: true
  # 本地测试环境特殊验证码
  test_otp_code: "123456"
